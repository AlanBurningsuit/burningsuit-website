# build output
dist/
.astro/

# dependencies
node_modules/

# test artifacts
playwright-report/
test-results/
/playwright/.cache/
.lighthouseci/

# design + source material — kept on disk locally, not tracked in the repo.
# The built site only needs the optimised derivatives already committed under
# src/assets; resources/ is the raw archive that prep-images.mjs draws from.
design/
resources/

# os / editor cruft
.DS_Store
Thumbs.db
*.local

# tooling scratch
.codex-serve*.log
skills-lock.json

# kept out of the (public) repo: internal orientation doc + brand source PDF
claude.md
Burningsuit Brand Guidelines V1.pdf
.agents/
.claude/
