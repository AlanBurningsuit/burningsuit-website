name: Deploy to GitHub Pages

# Builds the Astro site and publishes it to GitHub Pages. The site only goes
# live once the repo's Pages "source" is switched to "GitHub Actions" — until
# then this can run safely without touching whatever currently serves the apex.
on:
  push:
    branches: [main]
  workflow_dispatch:

# Least privilege for the OIDC Pages deployment.
permissions:
  contents: read
  pages: write
  id-token: write

# Let an in-flight production deploy finish rather than cancelling it.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
      # withastro/action runs npm ci + astro build and uploads dist/ as the
      # Pages artifact. Node 22 to match .nvmrc / package.json engines.
      - name: Build Astro site
        uses: withastro/action@v4
        with:
          node-version: 22

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
